interface:
  display_name: Dreamina Canvas Operation Recovery
  short_description: Observe and recover async runs by submitId; never resubmit without
    resubmittable=true
policy:
  allow_implicit_invocation: false
