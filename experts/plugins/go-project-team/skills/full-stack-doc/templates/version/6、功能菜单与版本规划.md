# {{PRODUCT_NAME}} 功能菜单与版本规划（{{VERSION}}）

> **模板职责**：从产品总菜单中冻结 `{{VERSION}}` 实际交付的导航、页面、权限和功能范围。不要默认采用附录中的 Agent 控制台菜单。

## 1. 菜单规划输入

| 文档 | 说明 |
| :--- | :--- |
| `5、{{PRODUCT_NAME}}-PRD文档-{{VERSION}}.md` | PRD |
| `10、{{PRODUCT_NAME}}-功能菜单与版本规划.md` | 全产品菜单 |

## 2. 本版菜单矩阵

| 一级菜单 | 二级菜单/页面 | 功能范围 | 角色/权限 | 状态 | 来源 |
|---|---|---|---|---|---|
| {菜单} | {页面} | {本版功能} | {角色或权限} | {新增/调整/沿用/不做} | {PRD 条目} |

## 3. 导航与状态规则

- 明确默认首页、深链、面包屑、搜索、角标、空状态和无权限状态；
- 标明从上版迁移、重命名、合并、废弃的菜单及兼容方式；
- 每个功能必须能追溯到版本 PRD，并与功能与界面规划保持一致。

## 4. 版本冻结检查

- [ ] 本版菜单没有超出 PRD 范围。
- [ ] 页面、路由、权限和版本标记一致。
- [ ] 后续版本功能没有混入本版可用菜单。
- [ ] 所有重命名和迁移入口都有兼容说明。

---

## 附录 A：完整示例 — ExampleConsole Web 菜单

> **示例档案**：ExampleConsole。以下是 Agent 控制台的完整菜单案例，复制时必须按目标产品重新完成菜单归属和版本冻结。

> 本示例用于冻结 `{{PRODUCT_NAME}} Web` 的最终菜单结构。示例主语是服务器侧智能体安装、纳管、配置、监控、审计与回滚控制面。

### A.1 一级菜单 (Top-level Menu)

{{PRODUCT_NAME}} Web 一级菜单固定为：

1. **概览 Dashboard**
2. **实例 Instances**
3. **智能体 Agents Management**
4. **模型 Models**
5. **技能 Skills**
6. **工具 Tools**
7. **自动化 Automation**
8. **记忆库 Memory Management**
9. **设置 Settings**
10. **文档 Docs**

---

### A.2 菜单归位与整合规则

- **监控 & 沙盒**：不再作为一级菜单。`监控` 功能分散至 `概览` 与 `实例`；`沙盒`（WASM/Docker）整合进 `工具 Tools` 的执行环境管理。
- **日志审计**：功能拆分。`巡检与风险发现` 归入 `概览`；`操作审计与系统日志` 归入 `设置 Settings` 或 `概览` 的事件流。
- **自动化 & 记忆库 & 文档**：从原有的二级或深层菜单提拔为一级菜单，以突出 AI 原生能力。
- **安装向导 & 节点管理**：统一收敛至 `实例 Instances` 模块。

---

### A.3 详细功能分布

#### A.3.1 概览 Dashboard
- **平台总览**：全局资源、活跃实例、节点分布。
- **健康看板**：实例健康、节点健康、服务状态（原“监控”模块内容）。
- **风险告警**：巡检发现的风险、异常实例告警（原“日志审计”巡检部分）。
- **任务简报**：最近失败任务、最近审计事件流。

#### A.3.2 实例 Instances
- **实例概览**：所有引擎（OpenClaw / ZeroClaw / ExampleEngine）的列表与运行状态。
- **创建与部署**：
  - 安装向导：环境预检、安装依赖、配置生成、服务安装、启动验证。
  - 批量部署：多目标主机的一键下发。
- **运维控制**：升级、回滚、重启、日志查看、性能监控。
- **节点管理**：远程主机管理、执行器（Node Agent）安装与状态。

#### A.3.3 智能体 Agents Management
- **Agent 市场**：官方及社区提供的智能体角色模板。
- **我的智能体**：已配置的 Agent 列表。
- **套装模板**：多 Agent 协作套装（Swarm / Team）。
- **生命周期**：版本管理、分发至实例、升级/回滚策略。

#### A.3.4 模型 Models
- **全部模型**：统一的模型接入列表。
- **凭证中心**：API Key 管理、多平台厂商接入。
- **模型订阅**：官方/聚合平台接入模板、配额监控、注册引导。
- **模型代理**：统一网关、服务端代理、故障转移（Failover）、熔断恢复。
- **路由策略**：基于成本/速度/能力的自动路由。

#### A.3.5 技能 Skills
- **Skills 市场**：可供 Agent 调用的技能（Function Calling）商店。
- **已安装技能**：技能的分类管理、启停控制。
- **开发与调试**：技能在线测试、权限范围（Scope）定义。

#### A.3.6 工具 Tools
- **MCP 市场**：Model Context Protocol 兼容插件商店。
- **连接器 Connectors**：渠道（WeCom, Slack, Telegram 等）接入管理。
- **执行环境**：WASM 沙盒、Docker 隔离环境、OpenSandbox 配置。
- **工具链集成**：外部 API 与 SDK 的连接配置。

#### A.3.7 自动化 Automation
- **工作流编排**：可视化或代码化的任务流水线。
- **定时任务**：Cron 任务、周期性巡检触发。
- **事件触发**：基于 Webhook 或系统事件的自动化响应。
- **执行审计**：自动化任务的历史记录与成功率分析。

#### A.3.8 记忆库 Memory Management
- **记忆后端**：向量数据库（Vector DB）、图数据库（Neo4j）管理。
- **知识索引**：文档上传、网页抓取、RAG 索引构建。
- **记忆策略**：Recall 召回机制、记忆衰减与压缩、跨 Agent 共享。
- **数据管理**：记忆片段查看、手动干预与清理。

#### A.3.9 设置 Settings
- **平台管理**：RBAC 权限、安全设置、更新与版本、系统备份。
- **审计日志**：系统日志、操作审计、异常追踪（原“日志审计”核心）。
- **存储与目录**：安装路径、数据卷、缓存清理。

#### A.3.10 文档 Docs
- **知识中心**：安装手册、接入文档、模型能力矩阵。
- **帮助与支持**：FAQ、故障排查（Troubleshooting）、社区链接。
- **API 引用**：控制面 API 定义与 Swagger 文档。


---

## 附录 B：StoreOps PRD 节选（产品概述）

### B.1 产品概述 (Product Overview)

#### B.1.1 产品定位
> 简要描述产品的定位、目标用户、核心价值

**产品定位**：{例如：面向门店老板和师傅的XXX应用，帮助门店高效管理订单、师傅、客户等业务}

**目标用户**：
- **主要用户**：{例如：门店老板、工作人员}
- **次要用户**：{例如：师傅、运营人员}

**核心价值**：
1. {例如：提升订单处理效率，减少人工操作}
2. {例如：实时掌握门店运营数据，辅助决策}
3. {例如：优化师傅工作流程，提升服务质量}

#### B.1.2 产品目标
> 明确本次迭代要达成的业务目标

**业务目标**：
- {例如：提升订单处理效率30%}
- {例如：减少订单遗漏率至5%以下}
- {例如：提升师傅接单率至80%以上}

**用户目标**：
- {例如：门店老板能够快速查看和处理订单}
- {例如：师傅能够便捷抢单和上报服务进度}

#### B.1.3 产品范围
> 明确本次迭代包含和不包含的功能范围

**本次迭代包含**：
- {例如：工单模块（订单列表、订单详情、订单操作）}
- {例如：我的模块（门店信息、数据统计、待办事项）}
- {例如：首页模块（搜索、Banner）}

**本次迭代不包含**（后续迭代）：
- {例如：数据报表详细分析}
- {例如：客户管理模块}
- {例如：商品管理模块}

---

> **完整示例结束**
