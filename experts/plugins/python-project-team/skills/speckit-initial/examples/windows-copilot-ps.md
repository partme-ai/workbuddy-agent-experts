# Windows: Initialize with Copilot and PowerShell scripts

From the project root (PowerShell or cmd):

```powershell
specify init . --ai copilot --script ps
```

Use **speckit-check** to verify; then **speckit-constitution** or **speckit-specify**.
