# macOS: Initialize with Cursor

From the project root:

```bash
specify init . --ai cursor-agent
```

Then run `specify check` (or use **speckit-check**) to verify. Use **speckit-constitution** or **speckit-specify** next.
