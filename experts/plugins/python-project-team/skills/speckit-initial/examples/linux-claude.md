# Linux: Initialize with Claude

From the project root:

```bash
specify init . --ai claude
```

Verify with `specify check` (or **speckit-check**); then **speckit-constitution** or **speckit-specify**.
