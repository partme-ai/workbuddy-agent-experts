# {{PRODUCT_NAME}} 系统架构设计（{{VERSION}}）

> **模板职责**：只描述 `{{VERSION}}` 相对产品级系统架构的增量、约束和落地决策。不要复制根级架构全文，也不要默认采用附录案例的桌面端、Agent、Gateway、DDD/COLA 或具体技术栈。

## 1. 版本架构输入

| 输入 | 内容 |
|---|---|
| 产品级架构基线 | [{{PRODUCT_NAME}} Architecture](../8、{{PRODUCT_NAME}}-Architecture.zh_CN.md) |
| 版本范围 | [{{PRODUCT_NAME}}-PRD文档-{{VERSION}}](5、{{PRODUCT_NAME}}-PRD文档-{{VERSION}}.md) |
| 质量属性 | {性能、可用性、安全、合规、成本、兼容性目标及来源} |
| 变更约束 | {必须复用、必须替换、禁止变更和待验证的架构边界} |
| 决策证据 | {ADR、POC、容量数据、接口契约、测试或正式评审结论} |

## 2. 版本架构增量

按项目实际补充以下内容；不适用的部分写明原因后删除：

1. 版本目标、非目标和质量属性变化；
2. 新增、修改、废弃的组件及其边界；
3. 关键调用链、数据流、状态变化和失败处理；
4. API、事件、数据模型和兼容性契约；
5. 安全、部署、容量、可观测性、迁移和回滚方案；
6. 架构决策、替代方案、风险、验证证据和负责人。

> **裁剪规则**：先读取 [`applicability.md`](../../references/applicability.md)，仅启用项目证据支持的架构模式。

---

## 附录 A：完整示例 — ExampleConsole V1 架构

> **示例档案**：ExampleConsole。以下内容完整保留原案例，用于展示版本架构文档的深度；复制时必须重写产品事实、技术栈、接口、部署和版本依赖。

> **标准说明**：以下为 **ExampleConsole V1** 实例（`product-docs/ExampleConsole/V1/3、…`）全文，产品名已替换为 `{{PRODUCT_NAME}}`，`{{VERSION}}` 请替换为实际版本目录名。分版本架构应与根目录 `8、{{PRODUCT_NAME}}-Architecture.zh_CN.md`（legacy 9 全文合并）对照：**总纲在根目录，本文档写本版增量与范围**。
>
> 若需按 legacy 章节从零起草，可对照根目录 `8、{{PRODUCT_NAME}}-Architecture.zh_CN.md` 的同一套二级标题。

---


> 范围更新：V1 当前 canonical 架构只服务于 {{PRODUCT_NAME}} Web / Desktop / Node 三形态的三引擎安装闭环。若本文仍出现渠道、模型或平台扩张表述，以 [../25、{{PRODUCT_NAME}}-三形态部署与运行架构说明](../25%E3%80%81{{PRODUCT_NAME}}-%E4%B8%89%E5%BD%A2%E6%80%81%E9%83%A8%E7%BD%B2%E4%B8%8E%E8%BF%90%E8%A1%8C%E6%9E%B6%E6%9E%84%E8%AF%B4%E6%98%8E.md) 与 [../27、{{PRODUCT_NAME}}-安装兼容与联调执行基线](../27%E3%80%81{{PRODUCT_NAME}}-%E5%AE%89%E8%A3%85%E5%85%BC%E5%AE%B9%E4%B8%8E%E8%81%94%E8%B0%83%E6%89%A7%E8%A1%8C%E5%9F%BA%E7%BA%BF.md) 为准。

> **文档说明**：描述 **V1** 技术架构（多场景连接与编排：多实例管理 + 配置 + 渠道 + 模型）。V2/V3 架构见 [系统架构设计-V2](../V2/3、{{PRODUCT_NAME}}-V2-Architecture.zh_CN.md)、[系统架构设计-V3](../V3/3、{{PRODUCT_NAME}}-V3-Architecture.zh_CN.md)。

---

### A.1 文档信息 (Document Info)

#### A.1.1 版本记录
| 版本号 | 修改日期 | 修改人 | 修改内容 | 备注 |
| :--- | :--- | :--- | :--- | :--- |
| V1.0.0 | 2026-02-11 | ExampleOrg | 从总架构文档拆分为 V1 独立文档 | 仅含 V1 范围 |
| V1.1.0 | 2026-02-12 | ExampleOrg | 依据 PRD 完善：以 Gateway WebSocket 为主、基线冲突与离线策略、审计与企微凭证加密、性能与时区、术语与参考 | 与 PRD §5.7、§5、§6、§7 对齐 |

#### A.1.2 文档责任人
| 角色 | 职责 |
| :--- | :--- |
| 架构师 | 架构设计、技术评审 |
| 技术负责人 | 技术选型、与 OpenClaw/插件接口对齐 |
| 开发负责人 | 架构实现、接口契约落地 |

#### A.1.3 关联文档
| 文档类型 | 文档名称 | 说明 |
| :--- | :--- | :--- |
| PRD V1 | [{{PRODUCT_NAME}}-PRD文档-V1](5、{{PRODUCT_NAME}}-PRD文档-V1.md) | V1 功能需求、非功能与验收（权威依据） |
| 需求分析 V1 | [{{PRODUCT_NAME}}-需求分析文档-V1](2、{{PRODUCT_NAME}}-需求分析文档-V1.md) | 用户故事与功能规则细粒度依据 |
| 需求调研 V1 | [{{PRODUCT_NAME}}-需求调研文档-V1](1、{{PRODUCT_NAME}}-需求调研文档-V1.md) | 调研结论支撑 PRD |
| 架构 V2/V3 | `{{PRODUCT_NAME}}-V2-Architecture.zh_CN.md` / `{{PRODUCT_NAME}}-V3-Architecture.zh_CN.md` | 资源中心/RBAC（V2）、数据分析/扩展市场/多租户（V3）架构 |
| 实现参考 | [{{PRODUCT_NAME}}-技术方案与路线](../5、{{PRODUCT_NAME}}-技术方案与路线.md) | V1 平台能力与接口边界 |
| 子 PRD | 工作台/设备中心/模型服务/设置（见 PRD §1.3） | 各一级菜单功能与验收 |
| 总纲 | [{{PRODUCT_NAME}} Architecture](../8、{{PRODUCT_NAME}}-Architecture.zh_CN.md)、[{{PRODUCT_NAME}}-技术方案与路线](../5、{{PRODUCT_NAME}}-技术方案与路线.md) | 多引擎管理总纲与引擎适配层 |

---

### A.2 总纲：{{PRODUCT_NAME}} Desktop V1 架构（Tauri + OpenClaw/ZeroClaw）

与 [{{PRODUCT_NAME}}-产品与版本规划](../6、{{PRODUCT_NAME}}-产品与版本规划.md) V1 范围一致：**{{PRODUCT_NAME}} Desktop**，管理 **OpenClaw + ZeroClaw**，单机。

**应用层次**：前端（React/Vue）↔ **Tauri IPC** ↔ **Rust 后端**。

**Rust 后端模块划分**：

| 模块 | 职责 |
|------|------|
| engine_manager | 引擎适配层工厂，按 EngineType 返回 OpenClawAdapter / ZeroClawAdapter |
| openclaw_adapter | OpenClaw CLI 调用封装与 Gateway 协议（若需）；detect/install/create/start/stop/status/logs |
| zeroclaw_adapter | ZeroClaw CLI 调用封装；detect/install/create/start/stop/status/logs |
| process_manager | spawn/detach、PID 文件、kill -0 监控、可选自动重启 |
| config_generator | 根据 UI 表单生成 OpenClaw JSON / ZeroClaw TOML |
| instance_store | 本地 SQLite（如 tauri-plugin-sql）存储实例元数据（ID、引擎类型、配置路径、PID、状态等） |

**IPC 命令清单（`#[tauri::command]`）**：`detect_environment`、`install_engine`、`create_instance`、`start_instance`、`stop_instance`、`get_status`、`get_logs`、`list_instances`（及可选 `restart_instance`、`delete_instance`）。

**数据模型（概要）**：`InstanceConfig`（实例配置表单）、`EnvCheckResult`（环境检测结果）、`InstanceStatus`（运行状态、PID、内存等）、`InstanceInfo`（实例元数据，持久化在 SQLite）。

---

### A.3 架构概述 (Architecture Overview) — V1

#### A.3.1 系统定位

**系统定位**：{{PRODUCT_NAME}} 管理后台 = **Claw 引擎多实例管理平台**（多实例、配置、渠道、模型、工作台）。管理后台作为外置 Web 系统，通过 **Gateway WebSocket 协议**连接多台底层 OpenClaw / ZeroClaw 实例进行管理，并承载配置基线/模板/一致性/下发、渠道与模型配置、会话与用量聚合、日志与状态观测等能力；OpenClaw / ZeroClaw 为底层引擎，技术方案为实现与接口参考。平台不承载具体业务场景能力。

**系统目标（V1）**：与 PRD §2.1～2.3 一致。

- 支持多 OpenClaw 实例的注册、分组、健康监控与批量操作。
- 配置管理可落地：基线 + 模板 + 实例三层模型，一致性检测与批量下发，可审计；**基线版本冲突处理**（乐观锁，他人已更新则提示刷新并拦截保存）、**离线实例下发策略**（立即失败 / 自动重试，等待重试可查看与取消）。
- 单实例管理覆盖 OpenClaw 全部能力（概览/对话测试/渠道/会话/Agent/技能/节点/用量/定时/配置/调试/上下文诊断/日志），并以可视化表单替代 JSON 编辑。
- 工作台：在线实例数、总会话数、总消息数、告警数及异常实例告警待办（跳转设备中心）；会话与用量跨实例聚合；场景化运营由场景包提供。
- **审计日志**：下发记录必含**操作人**；配置变更与敏感操作可追溯（PRD §5.4）。
- **企微凭证加密存储**（PRD §5.2）；**错误响应与分页契约、时区**（PRD §5.7、§5.6）为架构约束。
- 管理后台可独立于 OpenClaw 部署，支持水平扩展与多环境（开发/预发/生产）。

#### A.3.2 架构原则

- **领域驱动 (DDD)**：设备/实例、配置管理、渠道接入、模型服务、会话与用量观测等按限界上下文划分；核心逻辑收敛在领域层。
- **依赖倒置**：应用层与领域层不依赖基础设施；通过 **Gateway WebSocket** 访问 OpenClaw 实例，数据库与缓存在 Infrastructure。
- **配置可落地**：基线/模板存 {{PRODUCT_NAME}} 数据库；下发通过 **Gateway WebSocket**（config.set/config.apply）写入并触发热重载；一致性由「基线 vs 实例当前配置」计算；**不使用 management/ics 插件的 REST API**（PRD §5.7）。
- **安全与审计**：管理后台鉴权（V1 Token）；配置与密钥不明文落日志；下发记录含操作人；企微凭证加密存储、前端全掩码。

#### A.3.3 技术选型

| 层次 | 选型 | 说明 |
| :--- | :--- | :--- |
| **管理后台前端** | Web：Element Plus / Avue / Echarts | B 端以 Web 为主，组件与 DNA 规范一致 |
| **管理后台后端** | Spring Boot (推荐 DDD/CQRS) 或 Node.js/TypeScript | 与 OpenClaw 同栈可加快 MVP；长期复杂业务建议 Java |
| **数据库** | PostgreSQL | {{PRODUCT_NAME}} 管理数据：实例、分组、基线、模板、下发任务、事件消息配置等 |
| **缓存** | Redis | 会话、热点配置、聚合统计缓存（可选） |
| **OpenClaw 插件** | TypeScript | openclaw_management、openclaw_ics、openclaw_wecom_kf（企微渠道） |
| **LLM / 向量** | OpenClaw 内置 + 多提供商 | Anthropic/OpenAI/国内模型；BM25 + 向量混合检索在 OpenClaw 侧 |

---

### A.4 系统架构 (System Architecture)

#### A.4.1 整体架构图

```
                    微信客户（C 端）
                          │
                ┌─────────▼─────────┐
                │  企微·微信客服系统   │
                └─────────┬─────────┘
                          │ HTTP 回调 / kf/sync_msg / send_msg
                          ▼
┌─────────────────────────────────────────────────────────────────────┐
│  OpenClaw Gateway（可多实例）                                         │
│  ┌─────────────────┐ ┌─────────────────────────────────────────────┐   │
│  │ openclaw_wecom_kf│ │ Gateway WebSocket（与官方 Control UI 一致）   │   │
│  │ 企微消息收发      │ │ config.get/set、sessions.list、health 等    │   │
│  │ 仅企微回调等 HTTP │ │ 配置/会话/健康等均通过 WS，无 management REST │   │
│  └─────────────────┘ └────────┬───────────────────────────────────┘   │
└────────────────────────────────┼───────────────────────────────────────┘
                                 │
                    WebSocket (Gateway 协议)
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│  {{PRODUCT_NAME}} 管理后台（独立部署）                                      │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  Web 前端（Vue 3 + Element Plus / Avue）                        │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                 │                                    │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  后端（Spring Boot 或 Node.js）                                 │  │
│  │  Adapter → Application → Domain ← Infrastructure               │  │
│  │  设备/配置/渠道/模型/会话观测 应用服务 + 领域模型                 │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                 │                                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────┐   │
│  │ PostgreSQL   │  │ Redis        │  │ OpenClaw 实例集群          │   │
│  │ 实例/基线/   │  │ 缓存/会话    │  │ Gateway WebSocket 连接     │   │
│  │ 模板/下发记录│  │ (可选)       │  │ 渠道配置/日志/状态观测服务 │   │
│  └──────────────┘  └──────────────┘  └──────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

#### A.4.2 逻辑架构 — COLA 分层

| 层次 | 职责 | {{PRODUCT_NAME}} 对应 |
| :--- | :--- | :--- |
| **Adapter（适配层）** | 处理外部输入输出 | Web Controller（工作台/设备中心/配置管理/模型服务/设置）；调用 OpenClaw 的客户端封装在 Infrastructure |
| **Application（应用层）** | 业务流程编排、用例 | 实例注册与分组、基线编辑与版本、一致性计算与展示、下发任务执行、渠道配置下发、会话/用量聚合查询 |
| **Domain（领域层）** | 核心业务逻辑与模型 | Instance、Baseline、ConfigTemplate、DeploymentTask、ChannelConfig；ConsistencyService、DeploymentExecutor |
| **Infrastructure（基础设施层）** | 技术实现 | PostgreSQL 仓储实现、Redis、**OpenClaw Gateway WebSocket 客户端**（按实例 WS 地址 + Token 连接，config.get/set、sessions.list、health 等）；日志采集、用量聚合、渠道回调校验等平台基础设施 |

**依赖方向**：Adapter → Application → Domain ← Infrastructure；领域层不依赖具体数据库与 OpenClaw 客户端，通过接口注入。

#### A.4.3 与 OpenClaw 的交互边界（与 PRD §5.7 一致）

- **管理后台不直接读写**：不访问 OpenClaw 所在机器的文件系统（openclaw.json、AGENTS.md、工作区等）。
- **以 Gateway WebSocket 协议为主**：管理后台与各 OpenClaw 实例的交互通过 **Gateway WebSocket 协议**（与官方 Control UI 一致），**不使用 management/ics 插件的 REST API**。配置读写、会话列表、健康检查等均通过 WS 方法（如 `config.get`/`config.set`/`config.apply`、`sessions.list`、`sessions.patch`、`health`、`status`）；bindings、eventMessages 通过 config 读写。
- **平台能力扩展**：渠道连通性校验、配置审计、日志聚合、实例诊断等由管理后台自有 API 承接；仅渠道回调等 HTTP 由对应插件暴露。
- **热重载**：配置类通过 config.set 写入后，由 OpenClaw 触发热重载（如 `gateway.reload.mode="hybrid"`），无需管理后台触发进程重启。

---

### A.5 核心 API 契约（以 Gateway WebSocket 为核心）— V1

管理后台与各 OpenClaw 实例的交互以 **Gateway WebSocket 协议**为主（与 PRD §5.7 一致）；平台侧关注配置、状态、日志、用量与运维任务，不承载具体业务场景能力。

#### A.5.1 Gateway WebSocket 方法

| 分类 | 方法 | 用途 | 说明 |
| :--- | :--- | :--- | :--- |
| 配置 | config.get | 读取实例当前配置 | 用于一致性计算、单实例编辑 |
| 配置 | config.set / config.apply | 写入配置并触发热重载 | 下发任务、从基线恢复、单实例保存 |
| 配置 | bindings、eventMessages | 路由与事件消息 | 通过 config 读写（结构在 openclaw.json 内） |
| 会话 | sessions.list | 会话列表 | 单实例会话查看、运行态排障 |
| 会话 | sessions.patch | 会话状态变更 | 接管/释放/转给坐席等 |
| 健康/状态 | health、status | 健康检查、版本、插件列表 | 工作台与设备中心状态展示 |

**连接与认证**：connect 时 `params.auth.token` 携带实例 Gateway Token；实例注册时在 {{PRODUCT_NAME}} 数据库中存储「WS 地址 + Token」（加密或脱敏），Infrastructure 层按实例建立 WS 连接并注入 Token。

#### A.5.2 管理后台自有 API 与扩展

| 能力 | 实现方式 | 说明 |
| :--- | :--- | :--- |
| 渠道配置校验 | 管理后台 API | 渠道凭证校验、回调连通测试、保存前预检查 |
| 日志与诊断 | 管理后台 API | 聚合日志、异常摘要、实例诊断结果 |
| 用量统计 | 管理后台定时采集 | 消息量、Token 用量、失败率等平台指标 |

#### A.5.3 接口契约（与 PRD §5.7、§5.6 一致）

- **错误响应**：`{ code, message, detail? }`；前端按 code 展示 PRD §2.4 规范（表单校验、操作成功/失败、网络异常、空状态）。
- **分页**：请求 `page`、`pageSize`；响应 `list`、`total`（或等价）；格式与 PRD 接口契约一致。
- **时间格式**：接口侧使用 **ISO 8601**（UTC）；前端按用户时区转换展示（PRD §5.6）。

---

### A.6 数据架构 (Data Architecture) — V1

#### A.6.1 管理后台自有库（PostgreSQL）

| 领域 | 表/用途 | 说明 |
| :--- | :--- | :--- |
| 实例管理 | instance, instance_group, instance_tag | 实例注册信息、分组、标签 |
| 配置管理 | baseline_version, config_template, deployment_task, deployment_task_result | 基线版本（含乐观锁版本号）、模板、下发任务及每实例结果；**deployment_task 必含操作人**（PRD §5.4） |
| 企微应用 | wecom_app_credential（或等价） | 企微应用凭证；存储时**加密**（如 AES-256-GCM），密钥与库分离（PRD §5.2） |
| 渠道配置 | channel_config_snapshot | 渠道凭证、回调地址、测试结果与下发快照 |
| 运行诊断 | runtime_diagnosis_snapshot | 实例诊断结果、错误摘要、修复建议 |
| 用量统计 | usage_metrics_snapshot | 消息量、Token 用量、失败率等平台指标快照 |
| 审计 | audit_log、deployment_task.operator_id | 配置变更与敏感操作日志；**下发记录必含操作人**；保存期限与界面可见性与 PRD §5.4 一致 |

基线/模板的「配置内容」建议 JSONB 或大字段存储，结构与 openclaw.json 对齐，便于 diff 与下发。

#### A.6.2 缓存（Redis，可选）

- 实例健康状态短 TTL 缓存，减少对多实例的频繁健康检查。
- 聚合统计（工作台概览）短 TTL 缓存。
- 会话列表/消息记录以实时查询 OpenClaw 为主，缓存按需引入。

#### A.6.3 数据一致性

- **基线/模板/下发记录**：强一致性，事务落库。
- **实例当前配置**：通过 management API 实时读取，与基线 diff 得到一致性状态；下发后再次读取验证或由下发结果接口返回。
- **跨实例会话/用量**：最终一致性；各实例数据源独立，管理后台聚合展示。

---

### A.7 安全架构 (Security Architecture) — V1

#### A.7.1 认证与授权

- **V1**：管理后台登录后使用 Token 或 Session；调用 OpenClaw 实例时使用各实例的 Gateway Token（存储与传递需加密或脱敏）。

#### A.7.2 数据安全（与 PRD §5.2 一致）

- 配置与密钥不明文写入日志；生产环境 HTTPS。
- **企微凭证加密存储**：corpSecret、token、encodingAESKey 必须加密落库（如 AES-256-GCM），密钥与库分离；**前端全掩码**（仅末四位可见）。
- 实例 Gateway Token 存库加密或脱敏；按实例调用时由 Infrastructure 注入。

#### A.7.3 访问控制

- 管理后台仅允许内网或 VPN 访问（视部署方式）；OpenClaw 实例的 endpoint 可为内网地址，通过管理后台所在网络可达即可。

---

### A.8 部署架构 (Deployment Architecture)

#### A.8.1 部署方式

- **{{PRODUCT_NAME}} 管理后台**：独立部署（单机或集群）；可容器化（Docker/K8s）；与 OpenClaw 实例无同机绑定。
- **OpenClaw Gateway**：每台实例独立部署（见技术方案 §2.3、§8.3）；支持多 Profile、多端口；生产建议 systemd/launchd 守护。
- **网络**：管理后台能访问各实例的 **Gateway WebSocket** 地址（与官方 Control UI 一致）；企微回调需公网可达，推荐 Nginx 反代 + HTTPS。

#### A.8.2 高可用与扩展

- 管理后台：无状态，可水平扩展；数据库与 Redis 做主从或集群。
- OpenClaw 实例：按业务需要多实例部署；会话与配置由各实例本地或共享存储承担（见 OpenClaw 文档）；管理后台仅做「配置下发」与「读状态/会话/用量」。

---

### A.9 监控与运维

#### A.9.1 监控

- **管理后台**：应用监控（QPS、RT、错误率）、关键接口（下发、一致性计算）耗时与成功率。
- **OpenClaw 实例**：各实例健康通过 **Gateway WebSocket** 的 `health`、`status` 获取；异常实例在「工作台」与「设备中心」告警展示。
- **日志**：管理后台与 OpenClaw 分别落日志；下发失败原因记录在下发记录中便于排查。

#### A.9.2 Gateway 部署检查清单（与技术方案 §8.3 一致）

| # | 事项 | 说明 |
| :--- | :--- | :--- |
| 1 | 安装 OpenClaw | 确认版本并锁定 |
| 2 | 配置 Gateway Token | 生产必须启用认证 |
| 3 | 安装 wecom-kf 等插件；Gateway 支持 WS | 企微消息收发；管理后台通过 Gateway WebSocket 连接，无需 management/ics REST |
| 4 | 外网可达 | 企微回调需公网 URL，推荐 Nginx + HTTPS |
| 5 | 服务守护 | macOS launchd / Linux systemd |
| 6 | 健康监控 | 集成 status/health 到监控与工作台 |
| 7 | 热重载确认 | gateway.reload.mode=hybrid |
| 8 | 备份 | 定期备份 ~/.openclaw/ 及 {{PRODUCT_NAME}} 数据库 |

---

### A.10 关键技术方案摘要 — V1

#### A.10.1 配置下发流程（可落地，与 PRD §5.2、§6 一致）

1. **基线编辑与保存**：用户在「配置管理」编辑基线；保存前**乐观锁校验**（后端基线版本 > 前端持有版本则提示「配置已被他人更新，请刷新后重新编辑」并拦截保存）；保存成功后版本递增。
2. **下发任务**：选择来源（基线/模板）与目标实例列表；Application 层生成下发任务记录，**操作人必填**并写入 deployment_task。
3. **离线实例策略**：若部分实例离线，支持 **模式 A 立即失败**（整任务失败或跳过离线实例由产品约定）或 **模式 B 自动重试**（最多 N 次，如 3 次）；等待重试的任务可查看与取消。
4. **执行下发**：对每个目标实例，Infrastructure 层通过 **Gateway WebSocket** 调用 `config.set`/`config.apply` 写入配置，触发热重载；汇总每实例成功/失败写回 DeploymentTask；前端展示结果；失败可重试并记录原因。
5. **一致性计算**：定时或按需对每个实例通过 WS `config.get` 拉取配置，与当前基线版本做 diff，更新「一致性状态」与差异项数。

#### A.10.2 历史场景层能力归档

历史业务场景能力已移出 {{PRODUCT_NAME}} 主线，本架构不再为其定义专属实现链路。

#### A.10.3 跨实例会话与用量聚合（与 PRD §5.4、§5.7 一致）

- 会话列表/消息记录：对每个实例通过 **Gateway WebSocket** 调用 `sessions.list`（及按需拉取单会话消息）；Application 层按实例/渠道/Agent/状态筛选与聚合后返回前端。
- 用量/健康：各实例通过 WS `health`、`status` 获取；工作台总览汇总各实例指标。

#### A.10.4 历史业务场景归档

历史业务场景能力已移出 {{PRODUCT_NAME}} 主线。当前系统架构仅要求会话查询、日志调试、用量采集、配置下发与状态监控链路可落地。

#### A.10.5 性能与压测（与 PRD §5.1 一致）

以下指标为架构约束与压测依据，须在约定测试条件下达成（或超出部分在发布说明中标注并规划优化）：

| 指标 | 目标值 | 测试条件 |
| :--- | :--- | :--- |
| 实例列表页首屏加载 | ≤ 2 秒 | 聚合 50 实例，每实例 3 个 Agent |
| 配置表单打开时间 | ≤ 1.5 秒 | 基线配置（7 大类表单） |
| 一致性总览加载 | ≤ 3 秒 | 100 实例，计算差异项 |
| 下发任务执行 | 批量下发 10 实例 ≤ 5 秒 | 含 WS 调用 + 热重载 |
| 单实例会话加载 | ≤ 2 秒 | 最近 50 条会话，单实例运行态查看 |
| 并发管理后台用户 | ≥ 20 人同时操作 | 主要页面无卡顿 |

#### A.10.6 时区与时间（与 PRD §5.6 一致）

- **接口**：统一使用 UTC、ISO 8601（如 `2026-02-12T08:30:00Z`）。
- **前端**：按用户浏览器时区转换展示；界面日期格式 `YYYY-MM-DD HH:mm:ss`（24 小时制）。

---

### A.11 历史桌面工作流归档

历史桌面工作流系统架构已从 {{PRODUCT_NAME}} 主线移除，本文件不再承载其技术架构设计。

### A.12 示例附录 (Appendix)

#### A.12.1 术语表（与 PRD §7.1 对齐）

| 术语 | 定义 |
| :--- | :--- |
| 实例 | 单台 OpenClaw Gateway，对应唯一 WebSocket 地址与 Gateway Token |
| 基线 | 全局期望配置，版本化存储于 {{PRODUCT_NAME}} 数据库；可编辑、乐观锁、导出与从模板应用 |
| 配置模板 | 可复用配置片段，用于应用到基线或实例；支持分类与版本管理 |
| 一致性状态 | 实例相对基线的状态：已同步（绿）/已修改（黄）/待下发（蓝）/下发失败（红）/未纳管（灰） |
| 下发任务 | 将基线或模板推送到选定实例的一次操作及结果记录；含操作人、离线实例策略 |
| Command Queue | OpenClaw 命令队列，可合并短时内多条消息为一个 Agent turn |
| 场景链接 | 带追踪参数的客服入口链接，用于多渠道引流和效果归因 |
| 历史场景协同视图 | 已移出主线的历史消息聚合视图 |
| 模型 Failover | 主模型超时/限流时自动切换备用模型 |
| 历史时效指标 | 已移出主线的历史业务时效指标 |

#### A.12.2 参考文档

- [{{PRODUCT_NAME}} PRD 文档 V1](5、{{PRODUCT_NAME}}-PRD文档-V1.md)（功能、非功能与验收；子 PRD 见 PRD §1.3：工作台/设备中心/设置/模型服务）
- [{{PRODUCT_NAME}} 需求分析文档 V1](2、{{PRODUCT_NAME}}-需求分析文档-V1.md)
- [{{PRODUCT_NAME}} 需求调研文档 V1](1、{{PRODUCT_NAME}}-需求调研文档-V1.md)
- 《{{PRODUCT_NAME}} 系统架构设计 V2》、《V3》


---

**文档版本**：V1.1.0  
**创建时间**：2026-02-11  
**最后更新**：2026-02-12  
**文档状态**：✅ 设计中 / 待评审，已与 PRD 对齐

> **完整示例结束**
