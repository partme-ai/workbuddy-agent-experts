# 删除项目反模式

| 错误做法 | 风险 | 正确处理 |
| --- | --- | --- |
| 根据相似标题猜项目 ID | 删除错误项目 | 用 `get_project` 验证完整资源名 |
| 把创建授权当删除授权 | 越过不可逆操作边界 | 展示目标后单独取得明确批准 |
| 超时后再次调用删除 | 重复不可逆写入 | 停止并用 `list_projects` 对账 |
| 仅凭成功文本宣布完成 | 远端状态可能不一致 | 用只读列表证明目标缺失 |

禁止批量删除、通配资源名、从日志复制未验证 ID，也禁止把对其他项目的批准复用到当前目标。
