# 本地验证示例

输入：目标 `projects/123`，读取结果标题“临时验收项目”。

批准前输出：将删除“临时验收项目”，纯 ID `123`，准确目标 `projects/123`。这是不可撤销远程操作；等待明确批准，尚未调用删除。

模拟批准后：只构造一次 `{"name":"projects/123"}`。模拟超时时不再次调用；`list_projects` 若仍含 `projects/123`，输出“未确认删除，停止”。
