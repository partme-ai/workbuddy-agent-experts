# language/ — Zig 语言核心

Zig 0.16.0 语言本身的深入参考。

| 文件 | 内容 |
|------|------|
| `../language.md` | 语言总览（comptime、错误处理、类型系统、内存） |
| `../builtins.md` | 内建函数（@as、@ptrCast、@import 等） |
| `../comptime.md` | 编译期执行（comptime 块、类型作为值） |
| `../patterns.md` | 惯用模式（error union、defer、errdefer） |
| `../code-review.md` | 代码审查清单 |
| `../illegal-behavior.md` | 未定义行为清单 |

> 历史原因：这些文件原本平铺在 `references/` 根目录。本索引仅用于主题归类，文件路径不变。
