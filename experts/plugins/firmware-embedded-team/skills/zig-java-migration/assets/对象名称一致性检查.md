# {{MODULE_NAME}} Java → Zig 对象与名称一致性检查

> Java 基线：`{{JAVA_BASELINE}}`  
> Zig 基线：`{{ZIG_BASELINE}}`  
> 最近审计：`{{AUDITED_AT}}`

## 规则

- 每个 Java 顶层对象对应一个主要 `.zig` 文件。
- 文件和目录使用 `snake_case`，类型使用 `TitleCase`，函数和变量使用
  Zig `camelCase`。
- `root.zig` 只做模块入口和重导出，不集中承载迁移对象。
- Java 可观察名称通过 registry、序列化、协议或兼容元数据保留。
- 命名适配不得改变参数、返回、错误、side effect 或调用链语义。

## 检查表

| Java 符号 | 期望 Zig 路径/符号 | 实际 Zig 路径/符号 | 名称适配边界 | 一致 | 状态 | 证据 |
|---|---|---|---|---|---|---|
| <!-- TODO --> | <!-- TODO --> | <!-- TODO --> | native/registry/serde/protocol | 否 | `MISPLACED` | <!-- TODO --> |

## 红线检查

| 检查 | 结果 | 证据 |
|---|---|---|
| `root.zig` 是否承载大量对象 | <!-- TODO --> | <!-- TODO --> |
| 是否用单一 `compat.zig` 充数 | <!-- TODO --> | <!-- TODO --> |
| 是否存在空 body、占位错误或滥用 `unreachable` | <!-- TODO --> | <!-- TODO --> |
| Java 动态名称是否在显式边界保留 | <!-- TODO --> | <!-- TODO --> |
| 文档与源码基线是否一致 | <!-- TODO --> | <!-- TODO --> |
