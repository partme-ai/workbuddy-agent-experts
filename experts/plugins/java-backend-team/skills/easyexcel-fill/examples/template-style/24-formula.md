# 模板中含公式

```java
// 模板中预置公式：=SUM(A2:A10)、=A2*B2、=REPLACE(...)
// 填充时 EasyExcel 不会重算公式，仅保留

// 模板设计示例：
// A2=单价  B2=数量  C2==A2*B2  ← 公式预置
// A3=单价  B3=数量  C3==A3*B3  ← 公式预置
// 填充时只需 fill 单价/数量，C 列由 Excel 自动算
```

> **官方原文**："函数（=A2&B2） 这里是不会被填充的"
