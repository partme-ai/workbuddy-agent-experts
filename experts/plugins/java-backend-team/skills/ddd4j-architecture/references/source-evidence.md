# Source Evidence

> Collection date: 2026-09-11. Re-read current source before relying on any path below.

- ddd4j parent POMs and BOM imports
- `io.ddd4j.core` SPI surface — `AggregateRoot`, `Command`, `Query`, `Repository`, `Contexts`
- Runtime registrations under `io.ddd4j.runtime.*`
- ArchUnit / `ddd-rules` tests in the current source tree
- CodeGraph queries for `port`, `adapter`, `SPI`, `Runtime`, and `Bootstrap`
