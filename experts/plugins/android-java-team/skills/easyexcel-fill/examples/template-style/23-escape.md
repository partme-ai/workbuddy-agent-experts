# 模板转义字符

```java
// 模板中含 { } 字面字符：用 \{ \} 转义
// 模板内容：状态: \{启用\}  →  替换为 "状态: {启用}"
// 不会被识别为变量

// 示例：模板中含 JSON 字符串
// 模板：{\"id\":{id}}  →  替换为 {"id":123}
```

> **官方原文**："如果本来就有'{'、'}' 特殊字符 用'\\{'、'\\}' 代替"
